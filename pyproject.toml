[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[project]
name="python-data-analysis"
dynamic = ["version"]
dependencies = [
    "pandas","numpy", "scipy", "matplotlib", "jupyterlab"
]