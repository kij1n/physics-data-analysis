[project]
name = "python-data-analysis"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pandas",
    "numpy",
    "scipy",
    "matplotlib",
    "jupyterlab",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
]
